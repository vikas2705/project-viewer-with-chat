{
  "buildCommand": "cd agent-ui && npm run build",
  "installCommand": "cd agent-ui && npm install",
  "outputDirectory": "agent-ui/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
